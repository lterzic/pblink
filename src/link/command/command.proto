syntax = "proto3";
package mp.pb.link;

import "link/command/copter.proto";

message Command {
    // Future-proof
    reserved 1, 2, 3, 4;

    // Vehicle specific commands
    oneof vehicle {
        CopterCommand copter    = 5;
    }
}